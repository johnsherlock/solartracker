# Solar Stats Delivery Backlog

This backlog is the working delivery tracker for the rewrite. It should be updated as work is completed or as discoveries introduce new items.

Execution discipline:

- Pull one active story at a time into `In Progress`.
- Every active story must have explicit acceptance criteria before implementation starts.
- Prefer finishing a vertical slice with verification and commit history before starting the next story.
- Keep "Current Priorities" short and map them to concrete backlog items wherever possible.

Status values:

- `Todo`
- `In Progress`
- `Blocked`
- `Done`
- `Deferred`

Epic tracking:

- When all active items in an epic are complete for the intended phase, mark the epic section heading with `Done`.
- Do not mark an epic done just because discovery notes exist; it should reflect meaningful completion of the scoped work.
- When GitHub Issues are adopted for execution, keep this Markdown file as the roadmap view and link issue IDs back into the relevant backlog items.

## Phase 1: Discovery and Product Definition `Done`

| ID | Epic | Title | Objective | Acceptance Criteria | Dependencies | Status | Notes / Discoveries |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D-001 | Discovery | Write product brief | Capture the product goal, target users, success criteria, and non-goals. | `docs/product-brief.md` exists and reflects the rewrite direction. | None | ✅ | Seeded from planning discussion. |
| D-002 | Discovery | Write use-case inventory | Capture the user questions and major product flows the new app must support. | `docs/use-cases.md` exists with core stories and acceptance notes. | D-001 | ✅ | Seeded from planning discussion and current-app audit. |
| D-003 | Discovery | Audit current app features | Capture current live-app behaviors and codebase features as reference material only. | Current feature set is summarized and gaps are noted for the rewrite. | None | ✅ | Live browser audit completed; current app remains reference only. |
| D-004 | Discovery | Gather financial evidence | Collect bills, tariff docs, and sample datasets for validation. | Source files are available in an agreed location and referenced from docs. | None | ✅ | Initial Energia bills and supplier CSV exports are present under `sample data/` and referenced from the calculation spec. |
| D-005 | Discovery | Define success metrics | Decide how we will judge the rewrite for product quality and correctness. | Product and engineering success metrics are documented. | D-001 | ✅ | Included in the product brief. |
| D-006 | Discovery | Define privacy expectations | Capture beta-user privacy, deletion, and operator-access expectations before implementation starts. | Privacy requirements are documented and reflected in architecture and backlog. | D-001 | ✅ | Seeded from user feedback on privacy and trust. |

## Phase 2: Financial Model and Validation `In Progress`

| ID | Epic | Title | Objective | Acceptance Criteria | Dependencies | Status | Notes / Discoveries |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | Financial Model | Create calculation spec | Turn product intent and current known rules into an explicit working spec. | `docs/calculation-spec.md` exists with known logic, open decisions, and required evidence. | D-001, D-002 | ✅ | Must be revised when bills and sample data arrive. |
| F-002 | Financial Model | Define "saving" | Decide what counts as savings and how it is presented. | Savings definition is written and approved in the calculation spec. | F-001, D-004 | ✅ | Savings now defaults to `withoutSolar.netCost - actual.netCost`, with export value still shown separately. |
| F-003 | Financial Model | Define no-solar baseline | Decide how the counterfactual bill is modeled. | Baseline logic is written with examples and edge cases. | F-001, D-004 | ✅ | Default now uses actual household demand as the baseline load shape rather than supplier import totals. |
| F-004 | Financial Model | Define export treatment | Decide whether export offsets import directly or is shown separately. | Export treatment is documented and testable. | F-001, D-004 | ✅ | Export is modeled as a separate credit line that rolls into net bill impact rather than negative import. |
| F-005 | Financial Model | Define fixed-charge handling | Validate standing charge, PSO, VAT, discount, and bill-period rules. | Fixed charges and tax treatment are documented with examples. | F-001, D-004 | ✅ | Fixed charges now default to date-ranged tariff properties present in both scenarios; remaining work is evidence validation, not rules definition. |
| F-006 | Financial Model | Define tariff versioning rules | Model plan changes across time without breaking history. | Date-ranged tariff rules are documented and examples span a plan change. | F-001 | ✅ | Tariff and fixed-charge versions now resolve at sub-billing-period granularity; Sept-Oct 2025 remains the key validation case. |
| F-007 | Financial Model | Build golden fixtures | Turn bills and sample datasets into repeatable validation fixtures. | Fixture set exists with expected outputs for multiple periods. | D-004, F-002, F-003, F-004, F-005 | ✅ | Superseded by Q-008, which covers fixture inputs with explicit expected financial outputs and tariff-transition coverage. Real-bill grounding folds into Q-002 and Q-003 when those are tackled. |
| F-008 | Financial Model | Define supplier-vs-solar reconciliation rules | Decide how supplier interval evidence and MyEnergi telemetry are compared when validating bills and usage models during development. | Reconciliation approach is documented, including expected mismatches and what source is authoritative for each validation use case. | D-004, F-007 | ✅ | Reconciliation is now defined as internal validation only, with supplier bills as authority and CSV/MyEnergi comparisons used for diagnostics. |
| F-009 | Financial Model | Define tariff validity versus contract dates | Separate tariff validity windows from contract end dates and define how reminders and recalculation should work. | The model distinguishes tariff validity from contract dates and supports retrospective correction. | F-006 | ✅ | Tariff validity now drives calculations, while contract dates remain reminder metadata only. |

## Phase 3: Data Model and Platform

| ID | Epic | Title | Objective | Acceptance Criteria | Dependencies | Status | Notes / Discoveries |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P-001 | Data Model | Choose initial platform direction | Confirm the application boundaries, local-dev expectations, and platform constraints without forcing a final hosting decision yet. | `docs/architecture.md` and decision records capture the current direction, explicit constraints, and deferred deployment choices. | D-001, D-002 | ✅ | The rewrite now assumes server-side provider access, summary-first persistence for v1, and serverless-compatible deployment, while final hosting remains deferred. |
| P-002 | Data Model | Define domain entities | Specify `User`, `Installation`, `TariffPlan`, `TariffPlanVersion`, `DailySummary`, and related operational records, while keeping interval persistence optional for later. | Architecture doc includes entity responsibilities and relationships. | P-001, F-006 | ✅ | Architecture now defines entity responsibilities plus first-pass supporting entities like fixed-charge versions and contract reminders; persisted interval readings are no longer assumed to be required for v1. |
| P-003 | Data Model | Define ingestion workflow | Describe scheduled import, normalization, summary generation, and health tracking. | Architecture doc includes ingestion and query flow. | P-001 | ✅ | Initial workflow documented. |
| P-004 | Data Model | Define local-dev approach | Ensure the new app can run locally without deployed Lambdas. | Local-dev expectations are documented in architecture notes. | P-001 | ✅ | Included in architecture doc. |
| P-005 | Data Model | Design persistence schema | Translate domain entities into relational schema and migration plan. | Schema proposal exists with tables, keys, and constraints. | P-002, F-006 | ✅ | Architecture now includes a first-pass Postgres schema proposal; a follow-up rewrite item is needed to align the executable schema with the summary-first v1 decision. |
| P-006 | Deployment / Operations | Centralize job execution and logs | Keep scheduled jobs, app runtime, and operational visibility within one coherent platform surface where possible. | Job strategy and logging approach are documented and avoid split-brain operations. | P-001, P-003 | Todo | Avoid dispersing logs across GitHub and hosting if beta scale grows to 100 users. |
| P-007 | Auth / Multi-user | Design privacy and deletion model | Ensure account deletion, data removal, and least-privilege access are first-class in the schema and architecture. | Deletion, retention, and access-control requirements are documented and mapped to entities. | D-006, P-002 | ✅ | Architecture now defines ownership boundaries, deletion workflow, retention defaults, and least-privilege access expectations. |
| P-008 | Ingestion | Define canonical energy model | Create a provider-agnostic internal day-detail and summary model so core logic is insulated from MyEnergi-specific payloads. | Canonical provider-facing response shapes and adapter boundaries are documented. | P-002, P-003 | ✅ | Canonical day-detail fields, summary inputs, and tariff-local-time assumptions are now documented as adapter concerns rather than browser or provider-specific concerns. |
| P-009 | Ingestion | Design provider adapter contract | Define how provider-specific import code maps raw payloads into canonical readings. | Adapter responsibilities and input/output contract are documented. | P-008 | ✅ | Architecture now defines adapter responsibilities, I/O shape, timezone handling, and MyEnergi-first assumptions without leaking provider schema into core logic. |
| P-010 | Data Model | Keep validation evidence outside the runtime ingestion model | Ensure supplier CSV and bill evidence can inform validation without becoming a product ingestion path or runtime dependency. | Architecture and schema keep runtime product data centered on provider telemetry and user-entered tariff/bill metadata, while dev-only validation tooling stays separate. | P-005, F-008 | ✅ | The rewrite app now lives separately under `apps/web`, with runtime schema/code focused on product data and supplier CSV evidence kept out of the app model. |
| P-011 | Deployment / Operations | Design provider health-check jobs | Define the hourly provider-health check, stale-data detection rules, and internal job flow for alerting users. | Health-check cadence, failure heuristics, and job responsibilities are documented. | P-003, P-006 | Todo | Start with hourly checks and email notifications. |
| P-012 | Auth / Multi-user | Design beta access request and invite flow | Define how prospective users request access, how admins approve them, and how invites are issued securely. | The beta request, approval, and invitation flow is documented end to end. | P-001, P-007 | Todo | Initial beta should stay gated and MyEnergi-only. |
| P-013 | Auth / Multi-user | Design user auth and terms acceptance | Define supported auth providers, approval-gated signup, and mandatory terms/privacy acceptance. | Auth and signup requirements are documented, including Google and terms acceptance. | P-001, P-007 | Todo | Keep onboarding low-friction but explicit about data use and deletion rights. |
| P-014 | Auth / Multi-user | Design account entitlements and gifted pro access | Define how premium entitlements work, including gifted pro access from an admin view. | Entitlement model supports gifted pro access and future premium gating. | P-001, P-007 | Deferred | Useful for testing premium features before adding payment flows. |
| P-015 | Data Model | Design provider-tenant growth model | Keep the data model compatible with future provider-level or white-label onboarding without making it core beta scope. | Architecture and schema notes identify how a provider or organization tenant could be added later. | P-002 | Deferred | Commercialization path for provider partnerships and bulk user onboarding. |
| P-016 | Deployment / Operations | Set up GitHub execution access | Enable issue and pull-request workflows from this environment so implementation work can be tracked and reviewed properly. | GitHub CLI or equivalent access is configured, authenticated, and verified for issue creation and PR workflows on this repo. | P-001 | Todo | High priority before active feature implementation so commits, issues, and PRs can be linked cleanly. |
| P-017 | Rewrite App | Scaffold separate rewrite application | Create a separate codebase for the rewrite inside the repo so legacy and new product work do not interfere with each other. | A standalone rewrite app exists with its own package boundary, build, and database configuration. | P-001 | ✅ | `apps/web` now contains a separate Next.js + Drizzle rewrite app that builds successfully without modifying the legacy root app. |
| P-018 | Rewrite App | Create first database migration baseline | Turn the schema proposal into an executable migration baseline for the rewrite app. | The rewrite app contains generated Drizzle migrations that match the current schema design. | P-005, P-017 | ✅ | Initial Drizzle schema and migration were added under `apps/web/src/db` and `apps/web/drizzle`; they now need a follow-up alignment pass for the summary-first v1 model. |
| P-019 | Rewrite App | Add first domain services over the schema | Start implementation of billing-oriented domain logic that uses the rewrite model rather than legacy code. | A first domain module exists for core billing/tariff behavior and is independent of the legacy app. | F-002, F-003, F-004, P-018 | ✅ | `apps/web/src/domain/billing.ts` now holds first-pass tariff resolution and billing comparison logic. |
| P-020 | Rewrite App | Align the rewrite schema with the summary-first v1 model | Revise the executable schema so v1 persistence centers on tariffs, installations, provider connections, daily summaries, and job/data-health state rather than persisted interval readings. | The Drizzle schema and migration plan reflect the current v1 data-flow decision; unnecessary interval-first tables are removed, deferred, or clearly marked non-v1; docs and code agree on the runtime model. | P-018, P-019 | ✅ | Removed `energy_readings`, `billing_comparisons`, `provider_raw_imports`; removed `source_reading_count` from `daily_summaries`; reset migration baseline to a clean `0000` with 11 tables. |
| P-021 | Rewrite App | Seed database with summary-first development fixture data | Create a lightweight seed path so the rewrite app can be exercised locally with a sample user, installation, tariff setup, representative daily summaries, and the finance metadata needed for day 1 payback reporting. | A repeatable seed command exists in `apps/web`; it inserts a coherent sample dataset spanning tariff windows and daily summary dates; if financed-installation duration is not yet represented cleanly, the story also adds the required schema field(s); documentation notes how to run it locally. | P-020, P-019 | ✅ | Completed and moved to `docs/stories/complete/P-021.md`. |
| P-022 | Rewrite App | Build the first summary-backed range-summary API | Expose one server-side path that loads persisted daily summaries and tariff data, resolves an installation-local date range, and returns a structured range-summary payload for Overview and History work. | A server-side route in `apps/web` reads from the rewrite schema, invokes the billing domain service, and returns a verifiable range-summary payload with completeness and tariff-change metadata for a requested period. | P-019, P-021, P-037 | ✅ | Pulled forward into `FE-005`; this is now the first production range-history backend slice rather than a vague later billing helper. |
| P-023 | Rewrite App | Build first provider-backed live/day-detail read path | Expose one server-side path that fetches provider data for today or a requested day, normalizes it server-side, and returns a provider-agnostic day-detail payload for the app. | A server-side route or page in `apps/web` can request a day from a provider adapter, keep provider details off the client, and return data suitable for the homepage live view or a single-day detail page. | P-003, P-009, P-017 | ✅ | Added `src/providers/v1/` adapter, `src/live/` normalizer and types, and `app/api/live/day` route. Verified against the live V1 endpoint with 20 unit tests passing. |
| P-024 | Rewrite App | Set up local Postgres development environment | Make the rewrite app runnable locally by defining and documenting a supported local Postgres setup, including required env vars and a simple path to run Drizzle commands. | A documented local-dev path exists for `apps/web` that covers installing or starting Postgres, creating a local database, setting `DATABASE_URL`, and successfully running `npm run db:push` plus `npm run db:seed`; any helper files such as `.env.example`, Docker Compose, or setup notes are committed and verified. | P-017, P-018 | ✅ | Added `apps/web/docker-compose.yml` (Postgres 16), `docs/local-dev.md` setup guide, and `apps/web/.gitignore`. Verified `db:push` and `db:seed` run cleanly against the Docker instance. |
| P-025 | Data Health / Reliability | Persist daily data-health summaries and outage incidents | Store derived per-day coverage and per-incident outage/anomaly records so uptime, trust, and calculation confidence can be reported over time rather than inferred only from the current day. | The runtime model includes persisted daily data-health summaries and outage/anomaly incidents keyed by installation and local date/time; reprocessing a day updates the existing records idempotently; the stored model can distinguish pre-install/no-data days from partial, stale, suspicious, and backfilled days. | P-020, P-023, U-012 | Todo | This is the foundation for month/year uptime reporting, confidence-adjusted savings/payback messaging, and historical outage review. |
| P-026 | Ingestion | Backfill historical provider coverage from installation date | Fetch and analyze historical provider day data from the installation date onward, or until the product can reasonably determine earlier dates are outside the usable history window, so trust and outage reporting are not limited to post-signup days. | A backfill path exists that can iterate day-by-day over provider data for an installation, derive daily health and outage incidents, stop or gate sensibly when no useful data exists before install/useful-history start, and persist the resulting coverage records for reporting and later refresh. | P-025, P-003, P-009 | Todo | Use installation date where available; where it is absent or unreliable, use conservative heuristics and make uncertainty explicit in downstream reporting. |
| P-027 | Deployment / Operations | Deliver outage and anomaly notifications | Turn persisted outage/anomaly signals into user-facing notifications, starting with simple email alerts and leaving room for future in-app digests or push notifications. | Notification rules are documented and implemented for significant provider outages/anomalies; alerts are deduplicated against persisted incidents; user-visible wording reflects uncertainty honestly; notification history can be audited against stored outage records. | P-011, P-025 | Todo | Keep `P-011` as the lower-level health-check/ops foundation; this item is the user-notification product layer over persisted signals. |
| P-028 | Frontend Implementation | Add adjacent-day caching, prefetch, and swipe-ready navigation support for Historical Day | Restore fast day-to-day browsing by caching single-day payloads, prefetching adjacent days, and using the same navigation backbone for button and swipe interactions on Historical Day. | Historical Day navigation reuses cached day payloads when available, prefetches adjacent eligible days, avoids future-date prefetches, and feels responsive on desktop and touch devices. | P-023, U-034 | ✅ | This is both a performance slice and the interaction backbone for touch-first historical browsing. |
| P-029 | Frontend Implementation | Extract shared single-day analysis components from LiveScreen | Refactor the Live screen into reusable single-day analysis components so Live and Historical Day can share charts and summary modules without duplicating one large page. | `LiveScreen` becomes a composition shell over extracted day-analysis modules, shared components support neutral historical copy, and the clock is decoupled from Live-only refresh behavior. | U-030 | ✅ | `P-027` was already taken in the backlog, so this enabling refactor is tracked as `P-029`. |

## Phase 2a: UI Discovery and Product Flows `In Progress`

| ID | Epic | Title | Objective | Acceptance Criteria | Dependencies | Status | Notes / Discoveries |
| --- | --- | --- | --- | --- | --- | --- | --- |
| U-001 | UX / Visual Design | Define app information architecture and screen inventory | Organize the new app around user decisions, not legacy dashboards, with a canonical screen list and top-level navigation model. | `docs/ui/screen-inventory.md` documents primary navigation, page list, screen ownership, and key edge states. | D-002, P-001 | ✅ | Completed in `docs/ui/screen-inventory.md`; top-level areas now include Overview, Live, History, Tariffs, Data Health, and Settings, plus supporting public/onboarding surfaces. |
| U-002 | UX / Visual Design | Map primary user journeys | Define the main front-end journeys before backend slices harden around the wrong assumptions. | `docs/ui/user-journeys.md` covers live monitoring, day review, range analysis, first-time setup, tariff maintenance, contract reminders, and trust/error states. | U-001 | ✅ | Completed in `docs/ui/user-journeys.md`, including progressive onboarding, unlock-later setup, and trust/data-health journeys. |
| U-003 | Historical Views | Create low-fi wireframes for core monitoring views | Define the structure of the live landing page, single-day historical view, and multi-day/range analysis views. | `docs/ui/wireframes.md` contains low-fi desktop and mobile wireframes for live, daily, and range monitoring experiences. | U-001, U-002, F-002, F-003 | ✅ | Completed in `docs/ui/wireframes.md`; live, daily, and range views are now documented as low-fi desktop/mobile layouts. |
| U-004 | UX / Visual Design | Create low-fi wireframes for setup and onboarding | Define the structure of beta access, signup shape, installation setup, provider connection, tariff setup, and finance/payback capture, while allowing progressive completion after first use. | `docs/ui/wireframes.md` contains low-fi desktop and mobile wireframes for onboarding and setup flows, including optional-step skip paths and setup-completion messaging. | U-001, U-002, P-012, P-013 | ✅ | Completed in `docs/ui/wireframes.md`; provider connection is now the only hard requirement, with optional steps skippable and clearly tied to unlocked product value. |
| U-005 | Tariff Management | Create low-fi wireframes for tariff and contract management | Define how users view, add, edit, and review tariff versions, contract dates, reminders, and recalculation actions. | `docs/ui/wireframes.md` documents tariff-plan list, tariff-version editor, contract details, reminder states, and recalculation UX. | U-001, U-002, F-006, F-009 | ✅ | Completed in `docs/ui/wireframes.md`; tariff overview, version editing, reminders, and recalculation messaging are now covered. |
| U-006 | UX / Visual Design | Define system states for trust and health | Specify empty, loading, error, stale, partial-data, and setup-incomplete states across dashboards and setup flows. | `docs/ui/screen-inventory.md` and `docs/ui/data-contracts.md` define required trust/health and setup-completion states per screen. | U-002, P-011 | ✅ | Completed across `docs/ui/screen-inventory.md` and `docs/ui/data-contracts.md`, including setup-incomplete prompts, savings gating, and backfill/trust states. |
| U-007 | UX / Visual Design | Define lightweight design foundations | Establish low-fi design rules for cards, charts, comparison blocks, alerts, forms, date controls, and responsive layout patterns. | `docs/ui/wireframes.md` and `docs/ui/data-contracts.md` define reusable UI patterns and the chart/content rules they imply. | U-001, U-003, U-004, U-005, U-006 | ✅ | Completed through the shared patterns in `docs/ui/wireframes.md` and the screen-level data contracts in `docs/ui/data-contracts.md`. |
| U-012 | Live Data | Surface current-day data health warnings | Show users when current-day data appears incomplete, stale, or suspicious. | Live and current-day views display clear warnings when health heuristics fail. | U-004, P-011 | ✅ | Completed and moved to `docs/stories/complete/U-012.md`. The Live screen now derives current-day health server-side, supports per-incident dismissals, exposes uptime, and keeps data-quality detail reviewable after dismissal. |
| U-013 | Tariff Management | Add tariff-validity and contract reminders | Notify users when tariff validity or contract review dates have passed and their setup may be stale. | UX supports reminders, warnings, and easy correction of tariff validity periods. | U-005, F-009 | Todo | The key risk is stale rates after a renewal window passes without a newer tariff version being entered. |
| U-014 | Auth / Multi-user | Build beta access request form | Let prospective users request beta access with email and provider/setup details. | A request form and admin review workflow are designed and scoped. | P-012 | Todo | Keep initial eligibility limited to supported providers, starting with MyEnergi. |
| U-015 | Auth / Multi-user | Design signup and common auth flows | Support approval-gated signup via Google and other common auth methods, with required terms acceptance. | Signup and login flows are documented with auth options and terms acceptance points. | P-013 | Todo | Must make privacy/data-use expectations clear during onboarding. |
| U-020 | UX / Visual Design | Refine low-fi wireframes into mid-fi product layouts | Turn the agreed low-fi wireframes into clearer product layouts with stronger hierarchy, content design, and interaction rules before high-fidelity visual polish begins. | `docs/ui/mid-fi-layouts.md` exists for the primary signed-in, onboarding, tariff, and trust screens, with labels, modules, interactions, and state behavior clarified enough to review flow quality. | U-001, U-002, U-003, U-004, U-005, U-006, U-007 | ✅ | Completed in `docs/ui/mid-fi-layouts.md`; this is now the source of truth between low-fi structure and later high-fidelity/mockup work. |
| U-021 | UX / Visual Design | Create first high-fidelity exploration for the Live screen | Produce a one-off high-fidelity visual exploration for the Live screen to help set the tone before switching to a more direct prototype-first workflow. | The Live screen has a polished visual exploration that can inform the first clickable prototype pass. | U-020 | ✅ | Completed as a one-off exploratory step; this does not imply a continuing hi-fi-only stage for the rest of the product. |
| U-022 | UX / Visual Design | Build polished clickable prototype for the Overview screen | Turn the low-fi and mid-fi Overview definitions into a polished clickable screen so the product’s default signed-in landing experience can be reviewed in context. | A polished clickable prototype exists for Overview on desktop and mobile, including setup-progress, savings-locked, and trust-warning states. | U-020 | Todo | Keep this summary-first and decision-first; avoid reintroducing dashboard density. |
| U-023 | UX / Visual Design | Build first polished clickable prototype for the Live screen | Turn the Live screen into the first polished clickable prototype, using the existing live hi-fi exploration as reference where useful. | A polished clickable prototype exists for Live on desktop and mobile, including healthy, stale, and warning states, with clear trust/freshness treatment. | U-020, U-021 | ✅ | Completed and moved to `docs/stories/complete/U-023.md`. |
| U-024 | UX / Visual Design | Build polished clickable prototype for the Daily History screen | Turn the Daily History screen into a polished clickable prototype that makes single-day understanding and bill impact easy to review. | A polished clickable prototype exists for Daily History on desktop and mobile, including tariff-missing and partial-day states. | U-020 | ✅ | Superseded — U-033/U-034/U-035 built the production Historical Day screen directly, making a prior prototype redundant. |
| U-025 | UX / Visual Design | Build polished clickable prototype for the Range History screen | Turn the Range History screen into a polished clickable prototype focused on period outcome first and breakdown detail second. | A polished clickable prototype exists for Range History on desktop and mobile, including tariff-change, missing-data, and savings-locked states. | U-020 | ✅ | Superseded — expanded into FE-006 (Range History Screen) with a direction story (U-040) and four implementation stories (U-041 through U-044), preceded by a data fix (P-039). |
| U-026 | UX / Visual Design | Build polished clickable prototype for the onboarding and setup flow | Turn the provider-first onboarding and progressive setup flow into a polished clickable prototype that can be reviewed end to end. | A polished clickable prototype exists for provider connection, installation setup, tariff setup, finance setup, and setup-review/progress states. | U-020, U-014, U-015 | Todo | Preserve the principle that provider connection is the only hard requirement for first use. |
| U-027 | UX / Visual Design | Build polished clickable prototype for the Tariffs overview screen | Turn the tariff-history and current-rate overview into a polished clickable prototype that makes “what applies now” obvious. | A polished clickable prototype exists for the Tariffs overview on desktop and mobile, including current tariff, historical versions, and reminder-warning states. | U-020, U-013 | Todo | Separate current truth from historical timeline clearly. |
| U-028 | UX / Visual Design | Build polished clickable prototype for tariff editing and contract reminders | Turn tariff version editing and contract/reminder management into a polished clickable prototype that feels safe and understandable. | A polished clickable prototype exists for the tariff version editor and contract-reminder flow, including overlap/validation, expired-validity, and recalculation-impact states. | U-020, U-013 | Todo | This should feel corrective and confidence-building, not administrative. |
| U-029 | UX / Visual Design | Build polished clickable prototype for the Data Health screen | Turn Data Health into a polished clickable prototype that makes operational trust actionable rather than abstract. | A polished clickable prototype exists for Data Health on desktop and mobile, including healthy, stale, partial, provider-error, and backfill-discovery states. | U-020, U-012 | Todo | Keep this screen diagnostic and reassuring, not like an internal log viewer. |
| U-032 | Data Health / Historical Views | Surface historical uptime, outages, and trust impact | Show users how provider availability has affected their usable history, savings confidence, and payback calculations across periods such as a month or year. | Product requirements define how daily coverage, outage periods, and anomaly incidents are surfaced in Data Health and reporting views; month/year uptime and missing-data summaries are understandable; savings/payback surfaces can explain when figures are based on incomplete history rather than silently presenting false precision. | U-029, P-025, P-026 | Todo | Include trust language such as “based on 10.3 of 12 months of usable data” and clear differentiation between no generation, no provider data, and pre-install periods. |
| U-033 | Historical Views | Build the first production Historical Day view | Create the first dedicated Historical Day experience in `apps/web` so past dates are reviewed as completed days rather than as modified Live screens, with fast navigation on desktop and touch devices. | Historical Day exists as a distinct page/route, hides realtime-only modules, uses neutral day wording, returns to Live when the user navigates back to today, and supports touch swipe navigation without breaking chart interaction. | P-029, U-034, P-028, U-035 | ✅ | Umbrella feature story for the first production Historical Day slice. |
| P-030 | Historical Views | Add client-side historical day API route and cache module | Create `GET /api/history/[date]` and a module-level `dayCache` so that historical day payloads can be fetched and stored client-side, enabling instant adjacent-day navigation without server round-trips. | API route returns full historical day payload as JSON; cache deduplicates requests; prefetch guards against future dates. | P-028, U-035 | ✅ | Infrastructure story — no visible UX change on its own; unlocks P-031. |
| P-031 | Historical Views | Convert HistoricalDayScreen to client-side navigation with day cache | Wire HistoricalDayScreen to the day cache so adjacent-day navigation is served instantly from memory; fall back to server render on cache miss; keep initial SSR load intact. | Cached navigation is instant with no loading state; URL stays correct; browser back/forward works; cache miss degrades gracefully. | P-030 | ✅ | Restores the SPA-feel of the V1 app for historical day browsing. |
| U-037 | Frontend Implementation | Mobile layout improvements for Live screen | Apply the same mobile layout improvements from U-036 to LiveScreen.tsx — compact control bar, hide secondary chrome on mobile, consistent with Historical Day treatment. DayAnalysis chart changes already applied via U-036. | Control bar is single row on mobile; energy trend chart visible above the fold; no clipping at 412px; desktop unchanged. | U-036 | ✅ | Live screen has the same structural mobile problems as Historical Day — wrapping control bar, verbose headers above the fold. |
| U-036 | Frontend Implementation | Mobile layout improvements for Historical Day and day-analysis charts | Fix the mobile layout of the Historical Day screen so the chart is visible above the fold on a 412×915 viewport, reducing wasted space in the control bar, page hero, chart header, series legend, and chart heights. | Control bar is a single row on mobile; chart is visible above the fold without scrolling; series legend is 2-column on mobile; chart heights are responsive; desktop layout is unchanged. | P-032, U-034 | ✅ | Currently ~470px of chrome appears above the chart on mobile, pushing it below the fold entirely. |
| P-032 | Frontend Implementation | Replace Recharts with ECharts for day-analysis charts | Swap the Recharts `<Area>` charts in `DayAnalysis` for `echarts-for-react` equivalents so chart transitions are canvas-rendered and match the smooth animation quality of the V1 app. | Energy trend, cost/value, and solar coverage charts render and animate smoothly on desktop and mobile; existing series visibility, resolution switching, and cost-view toggling all work as before; Recharts dependency is removed. | P-029 | ✅ | Recharts SVG animation is janky at 1440 data points; ECharts canvas rendering with notMerge transitions animates like butter. |
| P-033 | Live Data | Define free API provider contract for location, forecast, and sun context | Choose and document the zero-cost provider approach for geocoding, forecast, and sun-context inputs so later implementation can proceed without reopening API pricing, licensing, or payload-shape decisions. | Docs name the preferred free provider stack and fallback path; required normalized fields are defined for location, forecast, and sun context; caching rules and provider-specific risks are documented; Google Maps is explicitly out of scope and public Nominatim is explicitly rejected as the primary dependency. | U-031 | ✅ | Documented in `docs/decisions/0002-weather-location-provider-contract.md`. Preferred stack: Open-Meteo (forecast + sun events) + Geoapify free tier (geocoding). Sun position is formula-based. Fallback geocoder is Open-Meteo Geocoding API. |
| P-034 | Data Model | Design installation location model and geocoding persistence | Define how exact and approximate installation location is captured, normalized, persisted, and reused so Live can show weather and daylight context without geocoding on every page load. | Schema-level requirements are documented for canonical lat/lon, precision mode, raw input, label, provider metadata, and geocoded timestamp; re-geocoding triggers are defined; missing-location behavior for Live is documented. | P-033 | ✅ | Documented in `docs/decisions/0003-installation-location-model.md`. Defines 9 installation fields, exact/approximate precision semantics, submission-gated geocoding, atomic field updates, and missing-location Live behavior. |
| U-034 | Historical Views | Create the dedicated Historical Day page and content model | Build the user-facing Historical Day page using shared single-day components, with historical wording, appropriate modules, and touch-friendly navigation behavior. | A dedicated historical page/route exists for past dates; it shows day totals, value, trend, coverage, and notes; hides realtime and future-looking sections; and supports button/date-picker plus touch swipe navigation. | P-029, U-024 | ✅ | This is the core UX story that turns the existing day-detail data into a proper historical-day experience. |
| U-035 | Historical Views | Add first-pass Historical Day notes and interpretation | Replace the lower live/future-looking section with historical interpretation that explains the selected day using available day data and trust state. | Historical Day includes a calm notes/story block that reflects generation strength, grid reliance, export/self-consumption mix, and trust/tariff caveats without implying unsupported comparisons. | U-034, P-025 | ✅ | Rankings, percentile insights, and prior-year comparisons remain deferred follow-on work. |
| U-031 | UX / Visual Design | Reframe the Live screen around realtime state, live value, and solar context | Turn the first Live prototype review into a stronger second-pass screen direction that adds financial interpretation, richer controls, and site-aware solar context while preserving the realtime-first hierarchy. | A revised Live-screen story and prototype direction exist covering desktop/mobile, energy/value/context layers, and the key state variants. | U-023 | ✅ | Completed and moved to `docs/stories/complete/U-031.md`. |
| U-030 | Frontend Implementation | Build the first production UI slice in `apps/web` | Implement the first user-facing UI slice using the approved clickable prototypes and the rewrite app architecture. | At least one primary signed-in flow is implemented in `apps/web` with responsive layouts, documented states, and data wired to the corresponding server-side slice. | U-023, P-023, P-024 | ✅ | Completed and moved to `docs/stories/complete/U-030.md`. The Live route now runs against real server-side data, supports historical day selection, and no longer depends on prototype-only mock state. |
| U-038 | Live Data / UX | Design onboarding location capture and Live "Next" content model | Define the onboarding and Live-screen experience for capturing exact or approximate location and for presenting the 12-hour forecast rail, 5-day forecast cards, and solar-context modules in the lower `Next` section. | Onboarding copy covers address/eircode/postcode/zipcode and approximate-area entry; privacy messaging is defined; the 12-hour rail and 5-day card content are specified; desktop/mobile and missing-location/API-unavailable/after-sunset states are documented. | U-031, P-033, P-034 | ✅ | Documented in `docs/ui/live-next-section.md`. Covers onboarding form design, precision mode copy, geocoding outcomes, module hierarchy, slot/card content models, and full state table. |
| P-035 | Frontend Implementation | Add a normalized weather and sun-context server slice for Live | Define the server-side Live-context slice that loads persisted location data, fetches forecast inputs on a bounded cadence, computes current solar/daylight values, and returns a normalized payload for Live. | A clear contract exists for Live consumption; forecast cadence and cache TTL expectations are documented; geocoding does not run on every page load; daylight-left and current sun-position calculations are part of the slice; graceful degradation for partial API failures is documented. | P-033, P-034 | ✅ | Documented in `docs/decisions/0004-live-weather-server-slice-contract.md`. Covers location loading, Open-Meteo combined request structure, suncalc-based sun position, module-level cache with TTL + lat/lon invalidation, LiveWeatherResult union type, and degradation rules. |
| P-036 | Rewrite App | Build the first direct MyEnergi day-detail API for the rewrite | Replace the temporary V1 bridge with a rewrite-owned server-side MyEnergi adapter and API path for today or one requested historical local date. | `apps/web` can fetch a single installation-local day directly from MyEnergi, normalize it into the rewrite day-detail contract, and handle DST-edge days without reproducing the legacy proxy bug. | P-009, P-023, P-017 | ✅ | This is the direct-provider replacement for the temporary V1 day-detail bridge and must preserve the app-owned payload shape. |
| P-037 | Rewrite App | Build the Vercel-scheduled daily summary job for all active installations | Create the internal scheduled job path that runs after each installation's local midnight, fetches the finalized previous day, and upserts one `daily_summaries` row per installation/date. | A secure cron-triggered job exists in `apps/web`; it resolves the correct previous local day, writes idempotently, and records useful run outcomes. | P-020, P-036, P-006 | ✅ | For the first beta we can assume Ireland-only installations and run one cron shortly after local midnight GMT/BST, but the code must still resolve dates with `Europe/Dublin` and handle DST correctly. |
| P-038 | Rewrite App | Capture and persist MyEnergi credentials during onboarding | Create the rewrite onboarding path that stores one MyEnergi provider connection per installation and lets backend reads/jobs resolve per-user credentials from the database-linked connection context. | The onboarding plan captures MyEnergi username/password server-side, persists a provider connection with a credential reference, defines invalid-credential/reconnect behavior, and supports seeded dev credentials without committing plaintext secrets. | P-020, P-012, P-013 | Todo | Enables `P-036` and `P-037` to stop depending on deployment-level MyEnergi env vars. |
| P-039 | Rewrite App | Add rate-band import breakdown to daily summaries | Fix the gap where daily summary derivation discards the day/peak/night import split — classify each minute reading into its tariff band at write time and persist the band totals so the billing domain can produce accurate time-of-use cost calculations from daily summaries. | `daily_summaries` has `day_import_kwh`, `night_import_kwh`, `peak_import_kwh`, `free_import_kwh` columns; `deriveDailySummaryFields` classifies readings by tariff windows; billing domain uses banded rates when available and falls back to day rate otherwise; range API series exposes band kWh per day; seed and tests updated. | P-037, P-022 | ✅ | Prerequisite for FE-006 financial charts. V1 handled this in `energy-calculator.ts` `calculateTotals` with hardcoded windows; v2 uses per-tariff window config stored in the database. Free import column added but classification deferred until `free_import_rule_json` schema is defined. |
| U-039 | Frontend Implementation | Build the Live weather, sunrise/sunset, and sun-position modules | Replace the current lower-section placeholders on Live with production modules for the 12-hour forecast rail, 5-day forecast cards, sunrise/sunset, solar noon, daylight remaining, and current sun position while preserving current Live hierarchy and capability gating. | Implementation scope is documented clearly enough for later coding without reopening provider decisions; horizontal-scroll expectations are defined for the 12-hour forecast; exact/approximate/missing-location and partial-failure behavior remain coherent. | U-038, P-035, U-031 | Todo | Keep claims sky-relative only; no roof-orientation or panel-output promises in this feature. |
| U-040 | Range History | Range History content model and screen direction | Define the Range History screen modules, period selector design, chart selection, mobile/desktop layout, and all state variants before implementation begins. | `docs/ui/range-history.md` covers module hierarchy, period presets, chart design notes (including free import slot), and a full state matrix. | U-020 | ✅ | Can run in parallel with P-039. No code — outcome is a written direction spec. |
| U-041 | Range History | Range History page shell, period selector, and summary header | Build the Range History page route, period preset selector, KPI summary header, tariff-change callout, and completeness strip. | Page loads for the default preset; period selector triggers re-fetch; KPI values are correct; tariff-change and completeness callouts appear only when needed; all state variants render correctly. | P-039, U-040 | ✅ | Depends on P-039 for accurate banded KPI values and U-040 for layout direction. |
| U-042 | Range History | Energy charts — trend line and per-day stacked bars | Add the energy trend line chart (import/generation/export) and per-day stacked bar chart (import + generation breakdown) to the Range History page. | Both charts render and re-render on period change; data gaps show as line breaks; import bar stacks by rate band when P-039 data is available; redundant third bar from v1 is absent. | U-041, P-039 | Todo | Import rate-band stacking requires P-039 band data in the range API series. |
| U-043 | Range History | Financial charts — cost histogram and period breakdown | Add the per-day cost histogram (actual vs without-solar) and period cost breakdown donut (import cost / fixed charges / export credit / solar savings) to the Range History page. | Both charts render with banded billing when available; simplified label shown when falling back to day rate; no-tariff state replaces charts with a prompt card. | U-041, P-039 | Todo | Requires P-039 for accurate banded cost data. Free import slot reserved in donut but not active. |
| U-044 | Range History | Solar coverage, export ratio, and payback tracker | Add the solar coverage area chart, export ratio chart, and (for financed installations) payback progress bar to the Range History page. | Solar coverage and export ratio charts render with data gap breaks; payback bar renders only for financed installations and reflects the selected period's savings vs finance payments. | U-041 | Todo | Export ratio chart hidden when no generation data exists for the period. |
| U-008 | Historical Views | Add same-day-last-year comparisons | Preserve the idea of seasonal comparison insights as a later feature. | Use case and future requirements are documented. | U-003 | Deferred | Long-finger item, not needed for initial beta. |
| U-009 | Historical Views | Add best-day insights | Surface highest-generation or standout-performance days in chosen periods. | Future requirements are documented with candidate metrics. | U-003 | Deferred | Fun and useful, but not launch-critical. |
| U-010 | UX / Visual Design | Design annual wrap-up | Create a year-end summary concept with stats, highlights, and charts. | Future requirements are documented for later scoping. | U-002, U-003 | Deferred | Engagement feature for later. |
| U-011 | Live Data | Add forecast-informed outlook | Explore adding weather-informed projection for upcoming days. | Future requirements and data-source assumptions are documented. | U-004 | Deferred | Superseded by the concrete `FE-004` story set for Live weather and solar context; do not treat this as a separate active roadmap item. |
| U-016 | UX / Visual Design | Design periodic summary digests | Support end-of-day, week, and month summaries by email, push, or in-app delivery. | Requirements are documented for recurring and on-demand summaries. | U-002, U-003, P-011 | Deferred | Likely premium feature rather than beta launch scope. |
| U-017 | UX / Visual Design | Design AI insight experiences | Define AI-generated reports, optimization suggestions, and weather-informed reminders. | Future requirements are documented with clear boundaries between insight, prediction, and automation. | U-002, U-003, U-011 | Deferred | Premium feature; keep value proposition strong before implementation. |
| U-018 | UX / Visual Design | Design admin approvals and gifted access screens | Define admin UX for beta approvals, invites, and gifted pro access. | Admin flows are documented for approval and entitlement management. | U-014, P-014 | Deferred | Needed before broader beta ops and premium experiments. |
| U-019 | UX / Visual Design | Keep provider-whitelabel UX in mind | Ensure UX foundations could support provider-branded or provider-managed access later. | IA and design notes call out likely extension points for provider-led onboarding. | U-001, P-015 | Deferred | Commercialization path, not immediate product scope. |

## Phase 5: Quality and Delivery

| ID | Epic | Title | Objective | Acceptance Criteria | Dependencies | Status | Notes / Discoveries |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Q-001 | Testing / Quality | Define validation strategy | Decide how bills, fixtures, and UI checks will validate correctness. | Quality strategy is written and tied to backlog items. | F-007, U-001 | Todo | Should combine unit, fixture, and e2e checks. |
| Q-001a | Testing / Quality | Establish rewrite unit-test harness | Put a lightweight unit-test setup in place for the rewrite so domain logic can be tested as it is introduced. | The rewrite app has a working unit-test runner and at least one passing domain test suite. | P-017 | ✅ | `apps/web` now uses Vitest and has passing domain tests under `__tests__`. |
| Q-002 | Testing / Quality | Add billing parity tests | Ensure calculations match expected bill outcomes. | Golden tests exist for multiple billing periods. | F-007 | Todo | Core confidence gate before beta. |
| Q-003 | Testing / Quality | Add tariff transition tests | Ensure date-ranged tariff changes produce correct outputs. | Regression tests cover periods spanning tariff changes. | F-006, F-007 | Todo | High-risk edge case. |
| Q-004 | Deployment / Operations | Define beta release workflow | Document how the new app will be tested by you first, then invited beta users. | Release workflow and rollout criteria are documented. | P-001, Q-001 | Todo | Keep scope small and operational overhead low. |
| Q-005 | Testing / Quality | Define privacy and deletion verification | Ensure deletion and access controls are tested, not just documented. | Test plan includes account deletion, data isolation, and restricted operator access scenarios. | P-007 | Todo | Important for beta trust. |
| Q-006 | Testing / Quality | Create provider diagnostics and reconciliation analysis tool | Build a repeatable repo-side tool to compare supplier CSV evidence against MyEnergi API data by date, interval, tariff bucket, and billed cost when we need internal diagnostics. | A script or tool exists that fetches API data, normalizes it, compares it to supplier CSV and bill data, and outputs useful diagnostics for development and regression testing. | F-008, P-008 | Todo | This is internal validation tooling only, not product functionality, and is no longer a near-term product dependency. |
| Q-007 | Testing / Quality | Define beta-access and auth verification | Ensure approval-gated signup, invite flow, terms acceptance, and auth-provider behavior are tested. | Test plan covers request, approval, invite, signup, and entitlement edge cases. | P-012, P-013, U-014, U-015 | Deferred | Important once beta onboarding moves from planning into implementation. |
| Q-008 | Testing / Quality | Add seed-backed integration coverage for the first range-summary API | Verify that seeded tariff, installation, and daily-summary data can be loaded through the rewrite persistence model and produce the expected range-summary output. | A test exercises the first server-side range-summary slice against seeded or fixture-backed data and asserts the returned summary shape, key values, and completeness metadata. | P-021, P-022, Q-001a | ✅ | Pulled into `FE-005` so the first summary-backed range API has integration confidence from the start. |
| Q-009 | Deployment / Operations | Fix GitHub workflows for the `v2` rewrite line | Replace the legacy-root CI assumptions with workflows that validate the rewrite app on `v2`, and remove legacy-only code from `v2` once it is no longer needed for the rewrite. | PRs targeting `v2` run the rewrite app test/build checks; pushes to `v2` run the same checks; legacy-root build assumptions are removed from `v2` workflows; legacy-only app code and tooling are removed from `v2` so the branch reflects the rewrite product only; workflow scope is documented in-repo. | P-017, Q-001a | ✅ | `v2` workflows now validate `apps/web` on PRs and pushes, workflow scope is documented, and the legacy app has been moved under `V1/`. |
| Q-010 | Deployment / Operations | Define and enforce PR rules for the rewrite branch | Establish the repository rules we want for PR titles, required checks, and merge expectations on `v2`. | PR expectations are documented; workflow or branch-protection-compatible checks reflect those expectations; conventional PR title rules are either retained intentionally or replaced intentionally. | Q-009 | Todo | Keep this lightweight but explicit so contribution flow stays consistent. |
| Q-011 | Deployment / Operations | Add rewrite preview build workflow | Prepare CI to build a development version of the rewrite app for a dev environment on PRs or selected branch pushes. | A workflow exists that can build the rewrite app artifact for a dev target, with deployment trigger conditions documented even if the environment hookup is initially stubbed. | Q-009 | Todo | Start with build/package behavior before wiring in hosting credentials and deployment. |
| Q-012 | Deployment / Operations | Add rewrite deployment workflow for the main shipping branch | Ensure pushes to the shipping branch run tests and build the deployable rewrite app artifact for release. | The shipping-branch workflow runs the required tests, builds the rewrite app, and is structured to deploy the resulting artifact to the target environment. | Q-009, Q-010 | Todo | When branch strategy changes again, update this item to match the final shipping branch name. |
| Q-013 | Testing / Quality | Add validation coverage for forecast, geocoding, and solar-context time logic | Define the automated and manual validation coverage needed for Live weather and solar-context functionality so timezone, daylight, location-state, cache, and partial-failure behavior are tested explicitly. | Required checks are documented for DST, timezone correctness, sunrise/sunset edge cases, daylight-after-sunset behavior, approximate-location labeling, partial API failures, geocode miss handling, and cache behavior. | P-035, U-039 | ✅ | Closed without the documentation-only deliverable. Key risks addressed directly: pre-dawn/post-sunset label logic extracted to `formatDaylightStatus` with full test coverage; API failure degradation path covered in `getLiveWeatherContext.test.ts`. |

## Current Priorities

1. `FE-005` Plan and implement the rewrite-owned backend spine: direct MyEnergi day reads, nightly summary job, and summary-backed range API.
2. `Q-010` Define and enforce PR rules for the rewrite branch, including the conventional PR naming expectations we want to keep.

## Active Risks

- Financial logic may drift if billing evidence arrives too late.
- "No solar" baseline modeling can become hand-wavy without explicit examples.
- Tariff versioning affects schema, APIs, and reporting, so delays there create downstream churn.
- If the executable schema keeps drifting away from the intended v1 data flow, implementation work will build on the wrong persistence model.
- Letting backend slices harden before the UI and data contracts are defined could lock us into the wrong product shape.
- Splitting jobs and observability across providers too early may create operational blind spots for beta support.
- Privacy and deletion requirements touch schema, logs, storage, and support workflows, so delaying them increases rework.
- Even with one provider at launch, letting MyEnergi schema leak into core logic would make future ingestion sources much harder to add.

## Feature Tracker

| ID | Feature | Goal | Status | Supporting Doc | Notes |
| --- | --- | --- | --- | --- | --- |
| FE-001 | Live Single-User Slice | Deliver one end-to-end working Live experience in `apps/web` for a single seeded local user using seeded rewrite DB data plus the temporary V1 minute-data backend. | ✅ | [`docs/features/FE-001.md`](/Users/john/Documents/Projects/pv-manager/docs/features/FE-001.md) | Completed. The seeded Live route now runs end-to-end against rewrite DB context plus the temporary V1 provider bridge, including current-day health, uptime, persistent chart preferences, and real-day navigation. |
| FE-003 | Historical Day View | Deliver the first dedicated Historical Day experience in `apps/web`, with extracted shared day-analysis components, neutral historical wording, and fast date-to-date browsing on desktop and touch devices. | ✅ | [`docs/features/FE-003.md`](/Users/john/Documents/Projects/pv-manager/docs/features/FE-003.md) | This is the next product slice after the first Live feature: turn existing day-detail data into a real historical-day experience rather than continuing to overload the Live screen. |
| FE-004 | Live Weather and Solar Context | Extend the Live screen with free-API-backed weather forecast, sunrise/sunset, daylight, and sun-position context using privacy-aware location capture. | ✅ | [`docs/features/FE-004.md`](/Users/john/Documents/Projects/pv-manager/docs/features/FE-004.md) | Free APIs only; no paid Maps dependency. |
| FE-005 | Direct MyEnergi Backend and Daily Summary APIs | Replace the temporary V1 backend bridge with rewrite-owned MyEnergi day-detail APIs, a Vercel-scheduled daily-summary job, and a summary-backed range API. | Todo | [`docs/features/FE-005.md`](/Users/john/Documents/Projects/pv-manager/docs/features/FE-005.md) | This is the backend spine for the next rewrite phase and assumes the low-cost Vercel hosting path for v1. |
| FE-002 | Data Health and Coverage Trust | Persist outage/anomaly signals, backfill historical coverage, and surface uptime/trust impact in product reporting and notifications. | Todo | [`docs/features/FE-002.md`](/Users/john/Documents/Projects/pv-manager/docs/features/FE-002.md) | Existing `P-011` covers health-check jobs only; this feature groups the broader product and data-model work needed for outage reporting, historical trust, and confidence-aware savings/payback. |

## Current Feature Order

1. `FE-001` Live Single-User Slice
2. `FE-003` Historical Day View
3. `FE-004` Live Weather and Solar Context
4. `FE-005` Direct MyEnergi Backend and Daily Summary APIs
5. `FE-002` Data Health and Coverage Trust
