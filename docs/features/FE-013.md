# FE-013: AOB and Later Product Opportunities

## Goal

Hold the remaining backlog items that do not yet have a natural home in the
active feature set, so they are no longer orphaned while still staying clearly
outside the near-term roadmap.

## Status

`Deferred`

## Feature Completion Criteria

- Orphaned discovery and future-product items have a documented holding feature
- Later opportunities can be split back out into dedicated features once they
  become active roadmap work

## Included Stories

Stories currently parked here:

1. [`U-022`](../backlog.md) Build polished clickable prototype for the Overview screen
2. [`P-015`](../backlog.md) Design provider-tenant growth model
3. [`U-008`](../backlog.md) Add same-day-last-year comparisons
4. [`U-009`](../backlog.md) Add best-day insights
5. [`U-010`](../backlog.md) Design annual wrap-up
6. [`U-011`](../backlog.md) Add forecast-informed outlook
7. [`U-016`](../backlog.md) Design periodic summary digests
8. [`U-017`](../backlog.md) Design AI insight experiences
9. [`U-019`](../backlog.md) Keep provider-whitelabel UX in mind

## Notes

- `U-022` likely deserves its own dedicated feature later, but for now it sits
  here because it is not part of the currently outlined next-wave feature set.
- This is a holding area, not a recommendation to implement these items next.
