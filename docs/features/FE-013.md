# FE-013: AOB and Later Product Opportunities

## Goal

Hold the remaining backlog items that do not yet have a natural home in the
active feature set, so they are no longer orphaned while still staying clearly
outside the near-term roadmap.

## Status

`Deferred`

## Feature Completion Criteria

- Orphaned discovery and future-product items have a documented holding feature
- Later opportunities can be split back out into dedicated features once they
  become active roadmap work

## Included Stories

Stories currently parked here:

1. [`P-015`](../backlog.md) Design provider-tenant growth model
2. [`U-008`](../backlog.md) Add same-day-last-year comparisons
3. [`U-009`](../backlog.md) Add best-day insights
4. [`U-010`](../backlog.md) Design annual wrap-up
5. [`U-011`](../backlog.md) Add forecast-informed outlook
6. [`U-017`](../backlog.md) Design AI insight experiences
7. [`U-019`](../backlog.md) Keep provider-whitelabel UX in mind

## Notes

- `U-022` has now been promoted out into `FE-017`, which gives the signed-in
  landing experience a real feature home instead of leaving it parked here.
- `U-016` has now been promoted into `FE-018`, which gives recurring digests a
  real notifications home instead of leaving them as a generic later idea.
- This is a holding area, not a recommendation to implement these items next.
