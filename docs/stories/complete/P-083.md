# P-083: Migrate the rewrite to `johnsherlock/solartracker` on `main`

## Objective

Complete the repository cutover so the rewrite lives canonically in
`johnsherlock/solartracker` on `main`, with the obsolete Replit-era app
removed and the remaining workflow/docs assumptions updated to match the new
home.

## What Shipped

- `johnsherlock/solartracker` on `main` is now the canonical repo and shipping
  branch for the rewrite
- The old Replit-era app was removed from the canonical repo head
- Rewrite-era history was preserved rather than replaced with a one-shot import
- CI and workflow expectations now target `main`
- Follow-up cleanup removed lingering repo-name references from app/runtime
  identifiers and active delivery docs
- Repo-local handover notes document the cutover and current operating
  assumptions

## Out of Scope

- Preserving every historical path from the earlier repo lineage
- Keeping the Replit-era app in the active repo head
- Broad refactors unrelated to the repository migration

## Verification

- `origin` points at `git@github.com:johnsherlock/solartracker.git`
- The active branch is `main`
- Active workflow files target `main`
- Remaining references to the old repo name were removed from the app and
  active docs

## Notes

- The migration is complete; future deployment and launch work should treat
  this repository and branch as the only active delivery surface.

### Status: Done
